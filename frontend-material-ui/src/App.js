

const scheduleId = new URLSearchParams(window.location.search).get("s")
console.log("schedule id", scheduleId)

function App() {
  return (
      <div>
        ok
      </div>
  );
}

export default App;
